Sioux Falls Iron Herd — exported link page

Files:
  index.html   the whole page (HTML + CSS + JS inlined)
  images/      your uploaded avatar and background
  config.json  re-import this in the builder to keep editing

To publish: upload this folder to any static host
(Netlify, Vercel, GitHub Pages, Cloudflare Pages) or just open index.html.